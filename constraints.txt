# CI dependency lock (interim) — pins to the versions where the full test suite
# is green. The local dev env had drifted from pyproject's declared ranges
# (e.g. textual 7.5 vs the old `<3.0` cap), so a clean `pip install` resolved
# different libraries than local runs used. This file makes CI reproduce the
# known-good closure. Used via `pip install -e ".[...]" -c constraints.txt`.
#
# NOTE: this file is currently hand-seeded from a known-good env. Regenerate it
# reproducibly with `./scripts/lock-deps.sh` (resolves + runs the test suite in
# a clean venv, then freezes the GREEN closure). Run where PyPI is reachable.

# --- Web framework / ASGI (route-enumeration behaviour) ---
fastapi==0.128.0
starlette==0.50.0
uvicorn==0.40.0
websockets==15.0.1
watchfiles==1.1.1
python-dotenv==1.2.1
httptools==0.7.1
uvloop==0.22.1

# --- Pydantic ---
pydantic==2.12.5
pydantic-core==2.41.5
annotated-types==0.7.0
typing-extensions==4.15.0
typing-inspection==0.4.2

# --- HTTP stack ---
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.12.0
sniffio==1.3.1
idna==3.11
certifi==2025.11.12

# --- Dashboard / TUI (Textual markup behaviour) ---
textual==7.5.0
rich==14.2.0
markdown-it-py==4.0.0
mdit-py-plugins==0.5.0
linkify-it-py==2.0.3
uc-micro-py==1.0.3
pygments==2.19.2
click==8.2.1
psutil==7.2.1
platformdirs==4.5.1

# --- Optional features ---
prometheus-client==0.23.1
aiosqlite==0.22.1
PyYAML==6.0.3

# --- Test / lint toolchain (match the env where ruff/mypy/pytest are green) ---
pytest==8.4.2
pytest-asyncio==0.26.0
pytest-cov==7.0.0
coverage==7.13.1
pluggy==1.6.0
iniconfig==2.3.0
packaging==25.0
hypothesis==6.148.9
respx==0.22.0
ruff==0.15.4
mypy==1.19.1
mypy-extensions==1.1.0
types-PyYAML==6.0.12.20260518
